server:
  port: 9004

spring:
  application:
    name: cloudalibab-provider-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
    sentinel:
      transport:
        port: 8719
        dashboard: localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: "*"
